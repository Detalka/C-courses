#include <iostream>
#include <math.h>
/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char** argv) {double A,B,C,x,y,z,l;
l=(abs((A*x)+(B*y)+(C*z)))/sqrt(pow(A,2)+pow(B,2)+pow(C,2));
	return 0;
}
